{
  "name": "quantummodule",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://blackpenguin46.github.io/quantummodule/",  // Add this field for GitHub Pages
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "react-router-dom": "^6.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",  // This builds the app before deploying
    "deploy": "gh-pages -d build"  // This deploys the build folder to GitHub Pages
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
